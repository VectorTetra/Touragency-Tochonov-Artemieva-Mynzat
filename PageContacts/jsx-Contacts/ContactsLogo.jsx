function ContactsLogo(props) {
    return (
        <div className="container">
            <h1 className="container h1">{props.data.text}</h1>
            <hr className="container hr"></hr>
        </div>
    );
}